# Cypress Fixtures

This directory contains test data and configuration files used by Cypress E2E tests.

## Files

- `test-credentials.json` - Auto-generated by the seed script containing test organization, client, and user data
- Used by all Cypress tests to ensure consistent test environment

## Usage

Run `npm run seed:test` to generate fresh test data before running E2E tests.